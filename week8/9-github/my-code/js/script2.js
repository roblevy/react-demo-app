/* eslint-disable no-unused-vars */
function combineStrings(str1, str2) {
  return `${str1} -- ${str2}`;
}

function mergeStrings(str1, str2) {
  return `${str1}${str2}`;
}

function concatStrings(str1, str2) {
  return `${str1} +=+ ${str2}`;
}

// Rob's function here
function quadruplify(x) {
  return x ** 4;
}

function retrunStr1(str1) {
  return str1;
}

// Ellie's function here
function mergeAndUppercase(str1, str2) {
  return `${str1.toUpperCase()} ${str2.toUpperCase()}`;
}

function foo(a) {
  return a;
}

function bar(a, b) {
  return a + b;
}

console.log('The second js file is linked');
